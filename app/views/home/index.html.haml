-if user_signed_in?
	-#=link_to 'Logout', destroy_user_session_path, method: :delete, class: 'ui item'
	=render partial: 'home/shop'
-else
	-#=link_to 'Login', new_user_session_path, class: 'ui item'
	%h1 Home#index
	%p Find me in app/views/home/index.html.erb